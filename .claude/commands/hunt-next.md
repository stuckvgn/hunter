---
description: Show the highest-priority next action across all tracked bounty programs
allowed-tools: Bash(~/Desktop/hunter/tracker.py:*)
---

Run `~/Desktop/hunter/tracker.py next` and read the output. Then pick the single top-priority action for me and propose concretely what to run. If multiple actions tie, prefer policy review (cheapest) over active phases.

Do not execute the suggested command — just propose it and wait for me to confirm.
